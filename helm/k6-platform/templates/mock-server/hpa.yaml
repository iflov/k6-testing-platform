{{- if .Values.mockServer.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: mock-server
  labels:
    {{- include "k6-platform.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: mock-server
  minReplicas: {{ .Values.mockServer.hpa.minReplicas }}
  maxReplicas: {{ .Values.mockServer.hpa.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.mockServer.hpa.targetCPUUtilization }}
{{- end }}
